
import { VerticalBox } from "std-widgets.slint";
export component PageMain inherits VerticalLayout {
    // alignment: start;
    Rectangle {
        Text {
            text: "this is main page";
        }
    }
}
